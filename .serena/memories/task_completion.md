When task complete, typical verification commands are `pnpm run typecheck` and `pnpm build`, but user preference in `AGENTS.md` says do not run tests/lint/typecheck by default unless explicitly asked or needed for diagnosis. Update docs when durable architecture/API/workflow/convention changes land. Keep `lib/api-spec/openapi.yaml`, `lib/api-zod/src/generated/`, and `lib/api-client-react/src/generated/` aligned when API changes occur. After code file edits, repo guidance says run `graphify update .`.